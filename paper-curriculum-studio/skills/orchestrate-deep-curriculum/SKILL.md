---
name: orchestrate-deep-curriculum
description: Legacy entry point — delegates to paper-to-curriculum (multi-agent pipeline with backend artifacts, nbformat, optional Manim + web/R3F).
---

# Orchestrate deep curriculum (alias)

Use **`paper-to-curriculum`** for the full implementation-oriented workflow, artifact paths, and five expert agents:

1. **paper-ingestion-agent**
2. **curriculum-architect**
3. **math-visual-designer**
4. **notebook-builder**
5. **web-ui-builder**

Also load **`math-formula-visual`** and **`ui-ux-learning`** when formulas or web UI are in scope. Rules: **curriculum-agent**, **curriculum-quality**, **experience-design**.
