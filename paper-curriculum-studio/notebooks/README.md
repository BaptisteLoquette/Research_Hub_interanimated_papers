# Generated notebooks

`build_notebooks.py` writes lesson `.ipynb` files here (or to a path you pass with `--out`).

Keep this folder for outputs when using the plugin backend from its install location; in a dedicated project repo, point `--out` to your project `notebooks/` instead.
